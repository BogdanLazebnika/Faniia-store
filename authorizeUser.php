<?php
session_start(); // Розпочинаємо сесію (якщо ще не почато)

// Перевіряємо, чи існує ідентифікатор користувача в сесії
if (isset($_SESSION['user_id'])) {
    // Ідентифікатор користувача існує в сесії
    $user_id = $_SESSION['user_id'];
    echo "Ідентифікатор користувача: $user_id";
} else {
    // Ідентифікатор користувача не знайдено в сесії
    echo "Користувач не авторизований";
}
?>